# Coconut Disease Model Placeholder
# Place your trained TFLite model here: coconut_disease_model.tflite

This directory is configured in pubspec.yaml to include ML model files.

Once you have trained the CNN model:
1. Run: python ../../../ml_core/prepare_dataset.py
2. Train the model
3. Convert to TFLite: python ../../../ml_core/convert_to_tflite.py
4. Copy the .tflite file here
